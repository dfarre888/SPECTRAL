apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-web
spec:
  selector:
    app: spectral-operations
    component: web
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
